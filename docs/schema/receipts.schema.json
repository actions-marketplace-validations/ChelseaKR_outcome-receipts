{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "https://github.com/ChelseaKR/outcome-receipts/docs/schema/receipts.schema.json",
  "title": "Outcome Receipts manifest",
  "description": "The receipts.json manifest exported alongside a report. Every figure carries a receipt: the exact query, the slice it was computed over, the value, and how the slice hash was produced. A figure withheld by small-cell suppression carries suppressed: true and null for every numeric field, so a withheld cell is never readable as a count of zero. The schema is versioned; see docs/decisions/0005-receipt-canonicalization-and-schema.md and docs/SPEC-STABILITY.md.",
  "type": "object",
  "required": [
    "schema_version",
    "hash",
    "receipts"
  ],
  "additionalProperties": false,
  "properties": {
    "schema_version": {
      "description": "The manifest schema version. Consumers check this before re-deriving fields. 2.0 withholds a suppressed receipt's numerics as null; 1.0 wrote zeros there and could not be told apart from a true zero.",
      "type": "string",
      "const": "2.0"
    },
    "hash": {
      "description": "How every slice_hash was produced.",
      "type": "object",
      "required": [
        "algorithm",
        "digest_size",
        "canonicalization"
      ],
      "additionalProperties": false,
      "properties": {
        "algorithm": {
          "description": "The hash algorithm name.",
          "type": "string",
          "const": "blake2b"
        },
        "digest_size": {
          "description": "The digest size in bytes.",
          "type": "integer",
          "minimum": 1
        },
        "canonicalization": {
          "description": "The rule set that turns a slice into the hashed bytes. Bumped when those rules change.",
          "type": "string"
        }
      }
    },
    "receipts": {
      "description": "One entry per figure, sorted by metric_id.",
      "type": "array",
      "items": {
        "type": "object",
        "required": [
          "metric_id",
          "suppressed",
          "value",
          "display",
          "unit",
          "kind",
          "definition",
          "value_sql",
          "row_count",
          "slice_hash",
          "computed_at"
        ],
        "additionalProperties": false,
        "allOf": [
          {
            "description": "A withheld figure asserts no number: every numeric field is null.",
            "if": {
              "properties": {
                "suppressed": {
                  "const": true
                }
              },
              "required": [
                "suppressed"
              ]
            },
            "then": {
              "properties": {
                "value": {
                  "type": "null"
                },
                "row_count": {
                  "type": "null"
                },
                "slice_hash": {
                  "type": "null"
                },
                "column_names": {
                  "type": "null"
                }
              }
            }
          },
          {
            "description": "A published figure asserts a real number: no numeric field is null.",
            "if": {
              "properties": {
                "suppressed": {
                  "const": false
                }
              },
              "required": [
                "suppressed"
              ]
            },
            "then": {
              "properties": {
                "value": {
                  "type": "number"
                },
                "row_count": {
                  "type": "integer"
                },
                "slice_hash": {
                  "type": "string"
                }
              }
            }
          }
        ],
        "properties": {
          "metric_id": {
            "description": "The stable identifier of the metric.",
            "type": "string"
          },
          "suppressed": {
            "description": "Whether small-cell suppression withheld this figure. This is the field to branch on. When true, value, row_count, slice_hash, and column_names are all null and the figure asserts no number; when false, they carry the real values, including a genuine zero. A figure that does not exist has no entry in receipts at all. Suppressed, absent, and zero are three states with three distinguishable renderings.",
            "type": "boolean"
          },
          "value": {
            "description": "The numeric value the figure asserts, or null when the figure is suppressed. Null is not zero: it means the report declines to state this number, and a consumer must not read, plot, or sum it as a count.",
            "type": [
              "number",
              "null"
            ]
          },
          "display": {
            "description": "The canonical rendering the grounding gate matches on. A suppressed figure displays the redaction marker [SUPPRESSED]; the marker is a redaction sentinel, never translated, and is not a number the gate binds.",
            "type": "string"
          },
          "unit": {
            "description": "The unit selecting formatting.",
            "type": "string"
          },
          "definition": {
            "description": "The plain-language definition of what the figure counts.",
            "type": "string"
          },
          "caveat": {
            "description": "An optional qualifying note that travels with the figure and its receipt.",
            "type": "string"
          },
          "indicator": {
            "description": "The optional logic-model indicator this figure measures.",
            "type": "string"
          },
          "data_source": {
            "description": "The optional source system for the indicator data.",
            "type": "string"
          },
          "collection_frequency": {
            "description": "The optional cadence at which the indicator data is collected.",
            "type": "string"
          },
          "value_sql": {
            "description": "The query that produced the scalar value.",
            "type": "string"
          },
          "row_count": {
            "description": "The number of rows in the slice, or null when the figure is suppressed. For an ordinary count metric the row count is the withheld value again, so it is withheld with it rather than zeroed. A row_count of 0 means a query ran and matched nothing.",
            "type": [
              "integer",
              "null"
            ],
            "minimum": 0
          },
          "slice_hash": {
            "description": "The hex digest over the canonicalized slice, or null when the figure is suppressed. The all-zero sentinel marks a genuinely empty slice; it is deliberately not reused for a withheld one, because a hash of a guessed row set could otherwise be compared against a published one to confirm the guess.",
            "type": [
              "string",
              "null"
            ],
            "pattern": "^[0-9a-f]+$"
          },
          "column_names": {
            "description": "The slice's column names, in query order, or null when the figure is suppressed. Folded into the slice hash under canonicalization v1.",
            "type": [
              "array",
              "null"
            ],
            "items": {
              "type": "string"
            }
          },
          "kind": {
            "description": "Whether the figure is an activity count (output) or a change in condition (outcome).",
            "type": "string",
            "enum": [
              "output",
              "outcome"
            ]
          },
          "computed_at": {
            "description": "ISO timestamp from the injected clock. Not checked by verify.",
            "type": "string"
          }
        }
      }
    },
    "provenance": {
      "description": "Optional machine-readable attestation that no number was written by a model and the gate bound every number before export.",
      "type": "object",
      "additionalProperties": true
    },
    "artifacts": {
      "description": "Optional map of bundle-relative artifact path (report.md, trace.html, chart SVGs, and report.docx when `run --format docx` wrote one) to the sha256 hex digest of its bytes, recorded at export so `verify --bundle` can check that sibling files were not swapped. See docs/decisions/0006-whole-bundle-verification.md.",
      "type": "object",
      "additionalProperties": {
        "type": "string",
        "pattern": "^[0-9a-f]{64}$"
      }
    },
    "requirements": {
      "type": "object",
      "description": "Requirement coverage, present only when the report spec binds a requirement document. Absent means the spec made no coverage claim, which is not the same as answering zero requirements. See ADR 0013.",
      "required": [
        "schema_version",
        "document_path",
        "document_sha256",
        "counts",
        "requirements"
      ],
      "additionalProperties": false,
      "properties": {
        "schema_version": {
          "const": "1.0"
        },
        "document_path": {
          "type": "string",
          "minLength": 1
        },
        "document_sha256": {
          "type": "string",
          "pattern": "^[0-9a-f]{64}$"
        },
        "counts": {
          "type": "object",
          "required": [
            "answered",
            "withheld",
            "unanswerable",
            "unanswered"
          ],
          "additionalProperties": false,
          "properties": {
            "answered": {
              "type": "integer",
              "minimum": 0
            },
            "withheld": {
              "type": "integer",
              "minimum": 0
            },
            "unanswerable": {
              "type": "integer",
              "minimum": 0
            },
            "unanswered": {
              "type": "integer",
              "minimum": 0
            }
          }
        },
        "requirements": {
          "type": "array",
          "items": {
            "type": "object",
            "required": [
              "requirement_id",
              "description",
              "status",
              "metric_id",
              "blocker",
              "reason",
              "detail"
            ],
            "additionalProperties": false,
            "properties": {
              "requirement_id": {
                "type": "string",
                "minLength": 1
              },
              "description": {
                "type": "string"
              },
              "status": {
                "enum": [
                  "answered",
                  "withheld",
                  "unanswerable",
                  "unanswered"
                ]
              },
              "metric_id": {
                "type": [
                  "string",
                  "null"
                ]
              },
              "blocker": {
                "type": [
                  "string",
                  "null"
                ]
              },
              "reason": {
                "type": [
                  "string",
                  "null"
                ]
              },
              "detail": {
                "type": "string"
              }
            }
          }
        }
      }
    }
  }
}
