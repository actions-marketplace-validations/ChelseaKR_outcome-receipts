name: codeql

on:
  # CI-CD-STANDARD §11e: PRs into main + weekly schedule; the former push:main
  # trigger was redundant with the pull_request run and is dropped.
  pull_request:
    branches: [main]
  schedule:
    - cron: "19 4 * * 3"
  workflow_dispatch:

permissions:
  contents: read

concurrency:
  # One idiom across the repository. Nothing has been lost here: this workflow
  # has no `push` trigger (§11e dropped it), so consecutive merges never
  # contended, and no `codeql` run on `main` has ever ended `cancelled`. What
  # the shared key still allowed was a `workflow_dispatch` evicting a pending
  # scheduled run on the same ref, and `codeql (python · actions)` is a required
  # status check. Keying non-pull-request events on the commit closes that and
  # leaves one rule to keep right instead of two.
  group: codeql-${{ github.ref }}-${{ github.event_name == 'pull_request' && 'pr' || github.sha }}
  cancel-in-progress: ${{ github.event_name == 'pull_request' }}

jobs:
  analyze:
    name: codeql (python · actions)
    runs-on: ubuntu-latest
    timeout-minutes: 30
    permissions:
      contents: read
      actions: read # analyze repository Actions workflows without granting write access
    steps:
      - uses: actions/checkout@34e114876b0b11c390a56381ad16ebd13914f8d5  # v4.3.1
        with:
          persist-credentials: false
      - uses: github/codeql-action/init@f205ea1c3313d32999d8d6a48b4f6530d4437b38  # v4.37.4
        with:
          languages: python,actions
          queries: security-and-quality
          config-file: ./.github/codeql-config.yml
      - uses: github/codeql-action/analyze@f205ea1c3313d32999d8d6a48b4f6530d4437b38  # v4.37.4
        with:
          upload: never
          output: codeql-results
      - name: Enforce zero CodeQL findings
        shell: bash
        run: |
          set -euo pipefail
          mapfile -d '' sarif_files < <(
            find codeql-results -type f -name '*.sarif' -print0
          )
          if (( ${#sarif_files[@]} == 0 )); then
            printf '%s\n' \
              '::error::CodeQL produced no SARIF files; refusing to pass without scan evidence.'
            exit 1
          fi

          for sarif_file in "${sarif_files[@]}"; do
            if ! jq -e '
              .version == "2.1.0"
              and ((.runs | type) == "array")
              and ((.runs | length) > 0)
              and all(.runs[]; .results == null or ((.results | type) == "array"))
            ' "$sarif_file" >/dev/null; then
              printf '::error::Invalid or empty CodeQL SARIF: %s\n' "$sarif_file"
              exit 1
            fi
          done

          finding_count=$(jq -s '[.[] | .runs[]?.results[]?] | length' "${sarif_files[@]}")
          if (( finding_count != 0 )); then
            jq -rs \
              '.[] | .runs[]?.results[]? | "\(.ruleId // "unknown rule"): \(.message.text // "finding")"' \
              "${sarif_files[@]}"
            printf '::error::CodeQL reported %s finding(s).\n' "$finding_count"
            exit 1
          fi

          printf 'CodeQL SARIF gate: %s file(s), zero findings.\n' "${#sarif_files[@]}"
      - uses: actions/upload-artifact@ea165f8d65b6e75b540449e92b4886f43607fa02  # v4.6.2
        if: always()
        with:
          name: codeql-results
          path: codeql-results
          if-no-files-found: error
